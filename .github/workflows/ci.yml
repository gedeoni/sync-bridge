name: CI Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test-and-lint:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
    
    - name: Set up Python
      uses: actions/setup-python@f67e243f3d9147414c5437faca82f4027459aa85 # v5.3.0
      with:
        python-version: '3.12'

    - name: Set up uv
      uses: astral-sh/setup-uv@f59ab4e9c70818d1844b20ec3f31317ba85a6a61 # v5.1.0
      with:
        enable-cache: true

    - name: Install dependencies
      run: |
        uv sync --group dev

    - name: Run Ruff Lint Check
      run: |
        uv run ruff check .

    - name: Run Ruff Format Check
      run: |
        uv run ruff format --check .

    - name: Run Automated Django Tests
      env:
        APP_AUTH_TOKEN: 'ci-secret-token'
        SECRET_KEY: 'ci-secret-key-for-testing'
      run: |
        uv run python manage.py test
